import React from 'react';
import { Box, Typography } from '@mui/material';


const Intro = () => {
    return (
        <Box>
            <img src={""} alt="Intro Image" />
            <Typography variant="body1">Text coming here</Typography>
        </Box>
    );
};

export default Intro;